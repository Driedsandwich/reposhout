# RepoShout 1.1.6 — 第7回監査への対応記録

作成: 2026-08-06 / 基準コミット: `e7eb27de2f447dcc34b81640f54f187270aa4f47`（main・タグ `v1.1.5`）

第7回監査（GPT-5.6 Sol Pro）の指摘6件への対応。**6件とも現物で再現した。**
今回は**P1が1件**あり、実際に値がXへ渡る欠陥だった。

> 公開状態（どこまで出したか）は [RELEASE_STATUS.md](RELEASE_STATUS.md) を見てください。
> この文書には書きません。書くと、マージやタグのあとに必ず古くなるからです。

---

## 1. 指摘と対応

| ID | 重大度 | 再現 | 直したもの |
|---|---|---|---|
| R7-001 | **P1** | ○ | 二重エンコードした資格情報らしきハッシュがXへ渡っていた |
| R7-002 | P2 | ○ | 確認待ちのデータ申告を、確定値として文書に出していた |
| R7-003 | P2 | ○ | 公開状態がまた古いまま（**3回目**）。検査が言い回しの一覧だったので素通りした |
| R7-004 | P2 | ○ | 自作のZIP読み取りがCRCを見ておらず、標準実装が拒否するZIPを通していた |
| R7-005 | P3 | ○ | `GITHUB_SHA` が無いと提出可否がfail-openしていた |
| R7-006 | P3 | ○ | 文字数保証の同義表現が残っていた |

### R7-001 — 二重エンコードした資格情報がXへ渡っていた（P1）

**再現**（出荷済みの 1.1.5 で確認）:

```
#client_secret=dummy          → ✅ 落ちた
#client_secret%3Ddummy        → ✅ 落ちた
#client_secret%253Ddummy      → ❌ そのまま残った
#client_secret%25253Ddummy    → ❌ 残った
#client%255Fsecret%253Ddummy  → ❌ 残った
#access_token%253Ddummy       → ❌ 残った
#state%253Ddummy              → ❌ 残った
```

`sanitizeFragment()` はフラグメントを**1回だけ**解いて `=` を探していた。二重エンコードだと
1回解いても `%3D` のままなので「`=` を含まない」と判定され、そのままXの投稿画面へ渡っていた。
**パス側は第4回監査で同じ穴を塞いだのに、フラグメント側が残っていた。**

**直したこと**: 規則を1つにした。

> 解いた結果が**まだ解ける形**なら、中身が何なのかこちらでは判定できない。判定できないものは載せない。

何重にエンコードされていても、この1つの規則で落ちる。壊れたエンコードも同じ理由で落ちる。

**最初に書いた実装は間違っていた。** 「変化がなくなるまで解くループ」を足したが、
その後ろに置いた「まだ解ける形なら落とす」の検査が先に効くので、**ループを1回に戻す変異を
入れてもテストが落ちなかった**。効いていない行を入れたことになるので、ループを外して
規則を1つにした。いまは検査を外すと回帰が2件落ちる。

**検査**: 鍵6種 × エンコード5通り × ページ3種の90件。正当なアンカー（行番号・コメント・
`#readme`・日本語見出し）が残ることも同じテストで見る（全部落としているだけではないこと）。

### R7-002 — 確認待ちの申告を確定値として出していた（P2）

`store/DATA_DISCLOSURE.json` では `Authentication information` と `User activity` を
`ownerMustConfirm: true` にしていたのに、**貼り付け用の表では確定した `No`** に見えていた。
貼る人は文書しか見ないので、確認待ちのものを確定値で出してはいけない。

正本では `answer` を `null` にして案を `proposedAnswer` へ移し、文書側にも **「要確認（案: No）」**
と出す。確認した設問文・確認日・選んだ答え・理由を書く `ownerConfirmation` の欄も用意した。

### R7-003 — 公開状態がまた古いまま（P2・3回目）

1.1.3（R5-005）、1.1.4（R6-005）に続いて**3回目**。1.1.4 で入れた再発防止は
「未リリース」「作業ツリーにあるだけ」という**言い回しの一覧**で見張る方式だった。
1.1.5 が「**作業ツリーのみ**」と書いた瞬間に素通りした。

**言い回しを増やしても、また別の言い方で同じことが起きる。** 方式を変えた。

| | 旧（1.1.4） | 新（1.1.6） |
|---|---|---|
| 何を見るか | 禁止する言い回しの一覧 | **git の実物** |
| 表に書くもの | 全部の版の状態 | **確定した版だけ** |
| 作業中の版 | 状態を書く（→必ず古くなる） | 書かない。`npm run release:status` を指す |
| タグが見えないとき | 黙って通す | **落ちる**（何も確かめていないと言う） |
| いつ走るか | main への push とPR | ＋**タグを打った直後**（そこでしか検証できない） |

確定した版については、①タグが実在するか ②タグの指す先が表の値と一致するか
③そのコミットが main にあるか ④CHANGELOG の見出しにタグ名が書いてあるか、を git に聞く。
「まだ」と書きようがない形にした。

### R7-004 — ZIPの読み手がCRCを見ていなかった（P2）

監査がCRCを1ビット変えたZIPを作ったところ、標準の `unzip` は `bad CRC` で拒否したのに、
こちらの読み手は「11ファイル、per-fileハッシュも一致」と通した。**読み手が甘いと、
壊れた成果物にも「検証しました」という報告が出てしまう。**

CRC32、中央ディレクトリとローカルヘッダの一致（名前・方式・CRC・サイズ・フラグ）、
圧縮方式の許可制、暗号化フラグ、重複名、危険な名前（`..`・絶対パス・バックスラッシュ）、
範囲外参照を検査するようにした。壊し方7通りで拒否できることを回帰にしてある。

あわせて、**自作の読み手だけに頼らない**。CIで標準実装でも展開して `manifest.json` が
出ることを確かめる（Ubuntu は `unzip -t` と展開、Windows は `System.IO.Compression.ZipFile`）。

### R7-005 — `GITHUB_SHA` が無いと提出可否がfail-openしていた（P3）

```javascript
if (ci.githubSha && sourceCommit !== ci.githubSha) { ... }
```

`ci.githubSha` が空や欠落だと突き合わせを飛ばし、提出可のままだった。**無いものは
無条件で通る**形になっていた。取り出したコミット・ツリー・`GITHUB_SHA` がいずれも
40桁の16進であることを条件に加え、分からないなら提出しない側へ倒す。

あわせて、手元の `npm run package` で通常名のZIPができると書いていた説明を直した
（1.1.5 から `NON-SUBMITTABLE` が付く）。

### R7-006 — 文字数保証の同義表現（P3）

「多く数える方向にしか外れない」「errs on the side of counting high」が残っていた。
禁止語を増やすいたちごっこをやめ、測った範囲を書く形へ統一した。

---

## 2. 走らせた検査

| コマンド | 結果 |
|---|---|
| `npm run test:unit` | **115件 PASS / 0 FAIL**（1.1.5 は106件） |
| `npm run test:e2e` | **10件 PASS / 0 FAIL** |
| `unzip -t` ＋ 展開 | 正常（`manifest.json` が直下に出る・11ファイル） |

環境: macOS（darwin 25.5.0）・Node v22.22.3・Chrome 151。

### 効いていることの確認（変異）

| 壊したもの | 落ちた検査 |
|---|---|
| 「まだ解ける形なら落とす」を外す | 資格情報の回帰 **2件** |
| `GITHUB_SHA` の判定をfail-openへ戻す | 提出可否 **2件** |
| ZIPのCRC・ヘッダ・方式・中身・長さを壊す（7通り） | すべて拒否されることを確認 |
| 確認待ちの欄を確定 `No` へ戻す | 申告の突き合わせ **2件** |
| `RELEASE_STATUS.md` の確定コミットを1文字変える | 公開状態 **1件** |
| 作業中の版を表へ書き足す | 公開状態 **2件** |

### 出来上がるZIP

| | |
|---|---|
| 名前（main への push で CI が作ったとき） | `reposhout-1.1.6.zip` |
| SHA-256 | `fb16b371e8651a062cedf36ac92ac34f04e9ad3c516ba353305c4d0142280838` |
| サイズ | 27,817 B / 11ファイル |

---

## 3. やっていないこと

- commit / push / PR / タグ / GitHub Release
- ストアへのアップロード・提出・掲載文の変更・データ申告の入力
- Xへの実投稿
- 実際のGitHub・実際のXの投稿画面での目視確認
- 監査者が以前作った参照されないオブジェクトへの操作（ref を作る・取り込む・掃除する）
