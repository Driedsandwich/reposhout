# 変更履歴

日付は JST。Chrome ウェブストアの公開版と、このリポジトリの版は別物なので分けて書く。

## [1.1.3] — 未リリース（GitHubにもストアにも出していない）

2026-08-05 の第4回監査で出た6件への対応。**うち1件（R4-001）は再現せず、指摘が誤りだった。**

### 直したもの

- **二重パーセントエンコードで機微ページの拒否を迂回できた** — `/%2573ettings/tokens` は1回しかデコードしていなかったため `%73ettings` までしか戻らず、設定ページと判定されなかった。変化がなくなるまで解いてから判定する（上限5回。解ききれないものは共有しない）。壊れたエンコードを拒否する挙動は維持
- **画面内Shareボタンがページ側の合成クリックでも動いた** — `btn.click()` を呼べば投稿画面を開けた。実害は「勝手に投稿される」ではなく「勝手に投稿画面が開く」だが、利用者の操作を起点にする設計が崩れるので `event.isTrusted` を見る。実拡張E2Eで、合成クリックでは開かず本物のマウス入力では開くことを実測している
- **未コミットの変更があっても「提出用」の名前でZIPを作れた** — 手元だけにある変更が入った成果物を、あとから履歴と突き合わせられない。汚れている場合は既定で失敗し、`--allow-dirty` を付けたときだけ `-dirty` 付きの名前で作る
- **`package-lock.json` の版が 1.1.1 のまま**だった。1.1.3 へ揃えた
- **PRIVACY の版表記が 1.1.1 のまま**だった。1.1.3 へ揃えた
- **ストアのPII申告** — Xへ渡すURLには必ずGitHubのアカウント名が入る（`github.com/<アカウント名>/…`）。この事実を PRIVACY と `STORE_DASHBOARD_CHANGES.md` に書き、PII欄は **Yes を推奨**へ改めた（判断材料も併記）

### 再現しなかったもの

- **R4-001（IPホストのURLで過少計算する）は再現しなかった。** 監査側は twitter-text を実行できず、公式仕様書から「IP URLはURLとして23で数えられるはず」と推定していた。こちらで公式実装を動かして測ると、`http://1.1.1.1` は **14**（素の文字数）、`http://[::1]` は **12** で、自前の計数と完全に一致する。11個並べた場合も164対164で差が無い。指摘の前提が事実と違った。
  ただし「測れる形にしておく」ことには意味があるので、IPv4・IPv6・ポート付き・繰り返しをオラクルの敵対的コーパスへ追加した。

## [1.1.2] — 2026-08-05 GitHubへ公開（タグ `v1.1.2`）・ストアには未提出

2026-08-05 の第3回監査で出た5件への対応。ここでも全件、修正前に現物で再現させてから直した。

### 直したもの

- **文字数をなお少なく数えていた** — 前版で「少なく数えない」と書いたが、それが誤りだった。`foobar.みんな/` を14（公式23）、`text:http://example.com` を23（公式28）、`http://foo_bar.com/…` を23（公式45）と数えていた。ドットを含むトークンを丸ごと23にしていたのが原因で、繰り返すと差が積み上がる。数え方を「URL候補の区切り方をすべて試して最大を採る」形へ変更した。公式がどう解釈しても下回らない
- **その保証を機械で検査するようにした** — 開発時だけの依存として `twitter-text` 3.1.0 を入れ、すべてのfixtureと生成した数千件の敵対的コーパスに対して「公式を1件でも下回ったら落ちる」テストを追加した。**配布物には第三者コードを入れていない**（収録一覧に無い）
- **資格情報らしきハッシュが残っていた** — `#access_token=` など決め打ちの名前しか見ておらず、`#client_secret=` `#password=` `#api_key=` `#session_token=` `#oauth_token=` `#refresh_token=` が素通りしてXへ渡りうる状態だった。名前を数え上げるのをやめ、**`=` を含むハッシュを名前によらず落とす**。壊れたエンコード・制御文字も落とす
- **エンコードしたパスで機微ページの拒否を迂回できた** — `/%73ettings/tokens` が設定ページと判定されず共有できていた。パスをデコードしてから判定し、デコードできない場合は共有しない
- **日本語のREADME見出しアンカーが落ちていた** — 64文字の上限に対し、「インストールと初期設定」はエンコードで100文字になる。上限を512文字にし、根拠を残した
- **ストア提出手順に旧説明が残っていた** — 「not transmitted anywhere」、データ申告を No のままでよいとする記述、「過去に審査を通った」を根拠にする記述を削除。データ申告は Web history / Website content とも **Yes を推奨**へ変更した
- **文書の言い回しを機械で見張るようにした** — 同じ種類の食い違いを3回の監査で3回とも指摘されたので、禁止する言い回しの一覧をテストにした

### 英語環境への対応（監査とは別件・2026-08-05）

アナリティクスで米国・Windows からの利用が複数あることを確認したため、その環境に合わせた。

- **画面内Shareボタンのツールチップが日本語で固定されていた**（`このページをXに投稿する`）。英語圏の利用者には意味の分からない文字列が出ていた。`_locales/`（英語・日本語）を追加し、ブラウザの表示言語に従うようにした。既定は英語
- 表示する文字を言語ファイルから取るようにし、コードへ直書きしていないことをテストで検査する。実拡張E2Eでも、表示言語に対応する言語ファイルと一致することを確かめる
- **CIに Windows の仕事を追加**した。単体テストと配布物の生成を windows-latest でも実測する（実拡張E2Eは openssl と Chrome の場所が環境依存なので Ubuntu 側のみ）
- Windows では <kbd>Alt</kbd>+<kbd>Shift</kbd> がキーボードレイアウト切替に当たるため、ショートカットの割り当て直しをREADMEへ案内した（既定値は変えていない）

### そのほか

- 配布物と一緒に `dist/release-manifest.json` を出すようにした（版・コミット・未コミットの有無・Nodeの版・ファイルごとのSHA-256・ZIPのSHA-256）
- 公式コーパスを実行していないので、テスト名や文書で「conformance／適合」と名乗るのをやめた

### 残していること

公式 `twitter-text` パーサーを拡張本体へ同梱してはいない（配布物が10倍以上になるため。本人判断）。実行時は自前の計数器で、公式との一致はテストで担保している。また、公式リポジトリの conformance コーパス（YAML）は npm パッケージに同梱されていないため実行していない。

## [1.1.1] — 2026-08-05 GitHubへ公開（タグ `v1.1.1`）・ストアには未提出

2026-08-05 の再監査（外部レビュー）で出た7件への対応。すべて修正前に現物で再現させてから直した。

### 直したもの

- **スキームの無いドメインを短く数えていた** — `example.com` を11、`a.co` を4と数えていたため、`a.co` を50個並べたタイトルを249と見積もって切り詰めず、X側では1199相当になって投稿できなかった。ドットを含むトークンは一律23として数える。公式より多めに数えることはあっても、**少なく数えない**（少なく数える方向だけが、Xに弾かれる文面を作る）
- **肌色修飾子つきの絵文字を過大に数えていた** — `✊🏽` `☝🏽` を4と数えていた（ベースがBMPだと絵文字と認識していなかった）。2になる
- **長いIssue/PR/Discussionで固定サフィックスが消えていた** — `(Issue #123 · owner/repo)` を含めてから末尾を切っていたため、識別に最も効く部分が真っ先に落ちていた。サフィックスぶんを先に確保し、可変のタイトル側だけを削る
- **認証・設定・管理画面を共有できてしまっていた** — クエリは落としていたが、タイトルとパスは X の下書きへ渡っていた。これらのページでは何も開かない（`buildShare` と `fallbackUrl` が `null`）。組織と Enterprise の管理画面も対象
- **リポジトリトップの節アンカーを常に落としていた** — `#readme` のような見出しアンカーは残す。`key=value` の形と長すぎるものは引き続き落とす
- **プライバシーの記述が実挙動と食い違っていた** — 「どこにも送信しない」は誤り。X の投稿画面を開く時点で、タイトルと正規化済みURLは X へ渡る。開発者のサーバーへ送らないことと、機能上 X へ渡すことを分けて書き直した
- **CIの供給網対策** — Action を可変タグから完全な commit SHA へ固定し、`permissions: contents: read` と `timeout-minutes` を明示

### 文書

- `CHANGELOG.md`（このファイル）を新設
- `store/STORE_DASHBOARD_CHANGES.md` を新設（ストアの掲載欄へ貼る差分）
- `store/LISTING.md` の「13ファイル」を実体どおり9ファイルへ訂正
- README・掲載文から「公式の定義どおり」という断定を外し、検査できている範囲の書き方へ改めた

### 残していること

公式 `twitter-text` パーサーそのものは同梱していない（配布物が22KBから250KB超へ増えるため、本人判断で見送り）。したがって公式 conformance コーパスは走らせていない。数え方は公式の規則に沿わせたうえで、ずれるときは必ず多めに数える側に倒してある。

## [1.1.0] — 2026-08-05 GitHubへ公開（タグ `v1.1.0`）・ストアには未提出

2026-08-04 の監査で出た4件への対応。

- 共有URLのクエリをページの種類ごとに扱う（意味のあるものは残し、認証・設定系はクエリもハッシュも落とす）
- X の文字数を twitter-text v3 の規則へ寄せた（既定2・重み1の4範囲・絵文字は書記素単位・URLは23・先にNFC）
- Esc で閉じてよい窓の判定を windowId だけにし、記録を `chrome.storage.session` へ置いた（`storage` 権限を追加）
- `npm ci` / `npm test` / `npm run package`、GitHub Actions、決定論的なZIP生成

## [1.0.1] — 2026-08-03 公開（ストアの現行版）

- Issue・PR ページにも画面内 Share ボタンを出す（Primer PageHeader のアクション枠）

## [1.0.0] — 2026-08-02 公開

- 初版。リポジトリページの Share ボタン、ツールバーアイコン、キーボードショートカット、Esc で共有ウィンドウを閉じる
