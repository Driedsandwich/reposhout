# RepoShout 1.1.7 — 第8回監査への対応記録

作成: 2026-08-06 / 基準コミット: `6e8434bd94a381e365aa7f9f8cbd16e8d19f60e4`（main・タグ `v1.1.6`）

第8回監査（GPT-5.6 Sol Pro）の指摘4件への対応。**4件とも現物で再現した。**

> 公開状態は [RELEASE_STATUS.md](RELEASE_STATUS.md)（確定した版の記録）と、
> GitHub の ref / CI（いまの状態の正本）を見てください。

---

## 1. 指摘と対応

| ID | 重大度 | 再現 | 直したもの |
|---|---|---|---|
| R8-001 | P2 | ○ | 更新なのに「新しいアイテムを追加」を案内していた |
| R8-002 | P2 | ○（構造は閉鎖済み） | データ申告2欄が本人の確認待ち。**AIが埋めてはいけない欄** |
| R8-003 | P2 | ○ | 解いたあとに壊れたエスケープが残るパスを共有していた |
| R8-004 | P3 | ○ | タグpushのCIが、いま付けたタグ自身を検証していなかった |

### R8-001 — 更新なのに新規登録を案内していた（P2）

`store/LISTING.md` は「初回提出と更新提出の両方で使う」と書いたうえで、
**「新しいアイテムを追加」→ ZIPをドラッグ** をそのまま手順として載せていました。

RepoShout は `1.0.1` が公開中なので、これは**更新**です。新規登録の入口から入ると、
**別IDの拡張機能がもう1つ出来て**、いまの利用者・評価・自動更新は引き継がれません。
出してしまってから気づいても戻しにくい種類の間違いです。

手順を「既存アイテムを開く → **Package** タブ → **Upload New Package**」へ直し、
初回登録の手順は付録（§7）へ分けました。検査は、更新の節に `Upload New Package` と
拡張機能のIDがあること、その節で新規登録の入口を案内していないことを見ます。

あわせて掲載文の `every other GitHub page` を `other shareable GitHub pages` へ直しました
（認証・設定ページは意図的に拒否するので、「他のすべてのページで動く」は事実と違う）。

### R8-002 — データ申告2欄が確認待ち（P2・こちらの作業は無し）

`Authentication information` と `User activity` は、正本でも文書でも
「要確認（案: No）」のままです。前回入れた構造は正しく働いています。

**空のままにしてあるのは意図的です。** `ownerConfirmation` の4欄
（ダッシュボードで読んだ設問文・確認日・選んだ答え・理由）は、**本人が確認して埋める欄**で、
AIが推測で確定してはいけません。ここが埋まるまでストアの Privacy practices は完成しません。

### R8-003 — 解いたあとに壊れたエスケープが残るパスを共有していた（P2）

**再現**（出荷済みの 1.1.6 で確認）:

```
入力  : https://github.com/%2573ettings%2525ZZ/tokens
解く  : %73ettings%25ZZ → settings%ZZ で停止（有効な %xx が無くなるため）
判定  : kind=repo / repo=settings%ZZ/tokens / sensitive=false
結果  : 共有できていた（fallback からも漏れる）
```

`pathSegments()` は「有効な `%xx` がある間」だけ解き、残った壊れたエスケープを拒否して
いませんでした。コードには「デコードできない場合は判定不能として null を返す」と
書いてあるのに、判定不能なものを共有できる側に置いていました。

同じ形で `https://github.com/%252e%252e/settings/tokens`（多重エンコードした `..`）も
経路の判定を狂わせられました。

**直したこと**: 経路を決めるのは先頭3つ（所有者・リポジトリ・種別）なので、
**そこに解ききれない `%` が残ったら共有しない**。`.` と `..` はどの位置でも拒否。
4つ目以降はファイル名なので、この規則は掛けません（`100%.md`・`a%20b.md`・
`%E6%97%A5.md` が共有できることを同じテストで確かめています）。

**適用限界**: 監査側と同じく、これらのURLが GitHub の実サービスで最終的にどう解決されるか
（404 / リダイレクト / 多層デコード）はこちらでも確認していません。直したのは
「判定できないものを共有できる側に置いていた」という**こちら側の穴**です。

### R8-004 — タグpushのCIがタグ自身を検証していなかった（P3）

タグpushでCIが走るようにはなっていましたが、走っていたのは「過去の確定済みの行を
git と突き合わせる」検査だけで、**いま付けたタグが正しいかは見ていません**でした。
別の版のタグを同じコミットへ付けても通る構造です。

タグpushのときだけ走るステップを足しました。

| 見るもの | 落ちる条件 |
|---|---|
| タグ名 | `manifest.json` の版と違う |
| タグの指す先（peeled） | 走っているコミット（`GITHUB_SHA`）と違う |
| そのコミット | `origin/main` に入っていない |

3つとも失敗で停止します（警告で流しません）。あわせて `RELEASE_STATUS.md` を
「いまの状態の正本」ではなく**確定した版の記録**と位置づけ直しました
（いまの状態の正本は GitHub の ref と CI）。

---

## 2. 走らせた検査

| コマンド | 結果 |
|---|---|
| `npm run test:unit` | **120件 PASS / 0 FAIL**（1.1.6 は116件） |
| `npm run test:e2e` | **10件 PASS / 0 FAIL** |

環境: macOS（darwin 25.5.0）・Node v22.22.3・Chrome 151。

### 効いていることの確認（変異）

| 壊したもの | 落ちた検査 |
|---|---|
| 先頭3つの `%` 検査を外す | 「解いたあとに壊れたエスケープが残るパスを共有しない」 |
| 更新手順を「新しいアイテムを追加」へ戻す | 「更新の手順が、新規登録ではなく既存アイテムへの差し替えになっている」 |
| タグ検証ステップを外す | 「タグpushのCIが、いま付けたタグ自身を検証する」 |

### 出来上がるZIP

| | |
|---|---|
| 名前（main への push で CI が作ったとき） | `reposhout-1.1.7.zip` |
| SHA-256 | `86e52fd07ea9f604e27ef228dc234651a99667a8e79dd08676f9692b7dfffc0c` |
| サイズ | 28,205 B / 11ファイル |

---

## 3. やっていないこと

- commit / push / PR / タグ / GitHub Release
- ストアへのアップロード・提出・掲載文の変更・**データ申告の入力**（R8-002 は本人の欄）
- Xへの実投稿
- 実際のGitHub・実際のXでの目視確認
- 壊れたエスケープを含むURLが GitHub 実サービスでどう解決されるかの確認
