# RepoShout 1.1.5 — 第6回監査への対応記録

作成: 2026-08-06 / 基準コミット: `0be7aaf48b71422be56863ef04e2043932be21d5`（main・タグ `v1.1.4`・tree `04d1fb0`）

第6回監査（GPT-5.6 Sol Pro）の指摘5件への対応。**5件とも現物で再現した。**
ランタイムの欠陥は0件で、残っていたのは**申告・説明・出荷判定・QA手順**のほうだった。

> 公開状態（どこまで出したか）の正本は [RELEASE_STATUS.md](RELEASE_STATUS.md) です。
> この文書には書きません。書くと、マージやタグのあとに必ず古くなるからです。

---

## 1. 指摘と対応

| ID | 重大度 | 再現 | 直したもの | 検査 |
|---|---|---|---|---|
| R6-001 | P2 | ○ | 提出してよい成果物の判定を、消去法から「条件を全部満たしたときだけ」へ | `test/package.test.mjs` に7件・`test/manifest.test.mjs` に1件 |
| R6-002 | P2 | ○ | ストアへ貼るホスト権限の説明を実装に合わせた | `test/docs.test.mjs` に2件 |
| R6-003 | P2 | ○ | 残っていた全称表現と、古くなった「走らせていない」を直した | `test/docs.test.mjs` に2件＋禁止語6語追加 |
| R6-004 | P2 | ○ | 提出物のQA手順に、内側ZIPを展開する工程を足した | `test/docs.test.mjs` に2件・`test/package.test.mjs` に1件（実物のZIPを読む） |
| R6-005 | P3 | ○ | 公開状態の正本を作り、欄を分けた | `test/docs.test.mjs` に3件 |

### R6-001 — 提出してよい成果物の判定が緩かった

**再現**: 判定はこうなっていた。

```javascript
const isPrBuild = ci.eventName === 'pull_request';
const submittable = !dirty && !isPrBuild;
```

「どれにも当てはまらないから true」という消去法なので、次が全部 `submittable: true` になる。

| 作り方 | 旧判定 | 新判定 |
|---|---|---|
| main への push | true | **true** |
| 手元で `npm run package` | true | false |
| main で `workflow_dispatch` | true | false |
| feature ブランチで `workflow_dispatch` | true | false |
| タグで `workflow_dispatch` | true | false |
| PRの検証ビルド | false | false |

`workflow_dispatch` は実行するブランチを画面から選べるので、これは机上の話ではない。
今回の main の成果物はたまたま正しかったが、判定がそれを保証していなかった。

**直したこと**: 満たすべき条件を並べ、**全部通ったときだけ true** にした。

1. CIのビルドである（手元のビルドではない）
2. `push` である
3. `ref` が `refs/heads/main`
4. 取り出したコミットが `GITHUB_SHA` と一致する
5. 未コミットの変更が無い

外れた理由は `notSubmittableBecause` に日本語で書く。名前にも出す（`…-NON-SUBMITTABLE.zip`、
汚れていれば `…-dirty-NON-SUBMITTABLE.zip`）。CIが成果物を残す条件も
`github.event_name == 'push' && github.ref == 'refs/heads/main'` に揃えた。

**この変更で、手元の `npm run package` は提出候補を作らなくなる。** 提出に使うのは
main への push で走ったCIの成果物だけ、という運用と一致する。

### R6-002 — ストアへ貼るホスト権限の説明が実装より小さかった

**再現**: `store/LISTING.md` の github.com の欄は、こう書いてあった。

```
The content script reads the page only to locate the button row, and adds a
single <li> element.
```

README と PRIVACY は 1.1.4 で直したのに、**実際に審査へ出す文面だけ**が旧説明のまま残っていた。
貼る文面ほど見落とす。

**直したこと**: 実装どおりに書き直した。①GitHubのアクション行を探す ②`<style>` 1つと、
ボタン1個を包む要素1つ（コンテナに応じて `<li>` か `<div>`）を足す ③GitHub側の要素は
消しも置き換えもしない ④**利用者が本物のクリックで操作したときだけ** `location.href` と
`document.title` を読む（ページ側の合成クリックは `event.isTrusted` で拒否）⑤cookieも
フォームの値も読まず、解析もせず、独自の通信もしない ⑥ツールバーとショートカットは
`activeTab` の別経路であることを書き分けた。

検査は、この欄に必要な語（`action row` / `<style>` / `wrapper` / `<li> or a <div>` /
`location.href` / `document.title` / `event.isTrusted` / `activeTab`）が入っていることを見る。

### R6-003 — 全称表現が残っていた

**再現**: 1.1.4 で「証明」という言い方は直したが、**別の言い回しが5か所残っていた**。

| 場所 | 残っていた表現 |
|---|---|
| `README.md` | `is only ever allowed to over-count` |
| `README.ja.md` | 多く数えることはあっても、少なく数えることはない |
| `store/LISTING.md` | ずれるときは**必ず多めに数える** |
| `src/share.js` | 公式がどの解釈を採っても、こちらがそれを下回らない |
| `README.md`（Known limitations） | The official conformance corpus … **is not run**（走らせるようになったのに古いまま） |

**禁止語の一覧に入れていなかったので、機械検査を素通りしていた。**
検出器の探索範囲が狭いと、範囲の外に書けば何でも通る。1.1.4 で足した検査は
「never under-counts」など自分が思いついた語しか見ていなかった。

**直したこと**: 6語を一覧へ足し、5か所を「測った範囲では過少計数は検出されていない／
有限の回帰検査であって全入力の証明ではない」という言い方へ直した。
逆向きの古さ（走らせているのに「走らせていない」）も別の検査で見張るようにした。
`store/LISTING.md` に残っていた古いテスト件数（40件）と日付も直した。

### R6-004 — 提出物のQA手順が1工程足りなかった

**再現**: ダウンロードして展開した成果物のフォルダの中身は、これだけ。

```
release-manifest.json
reposhout-1.1.4.zip
reposhout-1.1.4.zip.sha256
```

`manifest.json` が無いので、このフォルダを「パッケージ化されていない拡張機能」として
読み込むことはできない。手順は「その展開物をそのまま読み込み」と書いていた。
そのとおりにやると読み込めず、**リポジトリのフォルダで代わりに動作確認してしまう**——
つまり提出物とは違うコードをQAすることになる。

**直したこと**: 内側のZIPを**新しい空のフォルダへ展開する**工程を足し、
展開先の直下に `manifest.json` があることと版を確かめてから読み込む形にした。
やってはいけないこと（成果物のフォルダをそのまま読む／リポジトリで代替する／
手元で作り直したZIPを出す／ハッシュ未確認のZIPを出す）も明記した。

検査は2段構え。手順書に必要な工程が必要な順で書いてあることを見る検査と、
**実際にZIPを読んで「外側には `manifest.json` が無く、内側を展開すると出てくる」ことを
確かめる検査**（ZIPの読み手はテスト側に別途書いた。書く側の実装を使い回さない）。

### R6-005 — 公開状態の記述が古いままだった

**再現**: 1.1.4 は main へマージしてタグまで打ったのに、CHANGELOG の見出しは
「未リリース（作業ツリーにあるだけ・commit も push もしていない）」、実装報告の
「いまの状態」は main = 1.1.3 のままだった。

**根本原因**: 公開状態を、書いた時点で固定される文書（CHANGELOG・実装報告）に書いていた。
同じことを 1.1.3 でもやっている（第5回 R5-005）。**2回続けて同じ型で落ちた。**

**直したこと**: 公開状態の正本を `RELEASE_STATUS.md` に集め、
**main へマージ／タグ／GitHub Release／ストアへ提出／ストアで公開中**を別の欄にした
（タグを打つことと公開することは別の出来事なので、同じ欄に書くと必ず混ざる）。
自分で確かめるコマンドも併記した。CHANGELOG と実装報告には正本への参照だけを置いた。

再発防止は文書の書き方ではなく検査にした。**タグが実在する版について、
「未リリース」「作業ツリーにあるだけ」と読める書き方を禁止する。**
タグの有無は git に聞く。タグを取っていないチェックアウトでは何も確かめられないので、
CIの checkout に `fetch-tags: true` を足した。

### あわせて直したもの（監査の Task F）

認証情報の申告の書き方。「cookie・トークン・パスワードを読まない」は本当だが、
**操作された時点では現在のタブのURL全体をいったん受け取り**、認証・設定の画面かどうかは
そのあとで判定して拒否している。事実どおりに書き直し、`ownerMustConfirm` を true にして
本人が現行の設問文で最終確認する欄へ移した（`User activity` と合わせて2欄）。

---

## 2. 走らせた検査

| コマンド | 結果 |
|---|---|
| `npm run test:unit` | **106件 PASS / 0 FAIL** |
| `npm run test:e2e` | **10件 PASS / 0 FAIL**（実Chromeへ `Extensions.loadUnpacked`） |
| `npm audit` | 脆弱性 0件 |
| 秘密情報の走査（追跡＋未追跡 49ファイル） | トークン・鍵・個人メール・ローカルパス **0件**（必ず当たる対照を混ぜて53件ヒットを確認） |

環境: macOS（darwin 25.5.0）・Node v22.22.3・Chrome 151。

### 効いていることの確認（変異）

| 壊したもの | 落ちた検査 |
|---|---|
| 提出判定を旧式（PRでなく汚れていなければ可）へ戻す | `test/package.test.mjs` **7件** |
| CIのupload条件を「PR以外」へ緩める | 「PRのCIは提出候補の成果物を残さない」 |
| ホスト権限の説明を旧文へ戻す | 「ストアへ貼る github.com の権限説明が、実装と揃っている」 |
| QA手順から内側ZIPの展開工程を消す | 「提出物のQA手順に、内側のZIPを展開する工程がある」 |
| 1.1.4 の見出しを「未リリース」へ戻す | 「タグを打った版を『まだ出していない』と書いていない」 |

### 出来上がるZIP

| | |
|---|---|
| 名前（main へマージして CI が作ったとき） | `reposhout-1.1.5.zip` |
| SHA-256 | `dede98e4f2ecef86b81c497f4b99910d64b0391bf4eb0b2775689fc9cf9dd0dc` |
| サイズ | 27,561 B / 11ファイル |
| いま手元にあるもの | `reposhout-1.1.5-dirty-NON-SUBMITTABLE.zip`（**バイト列は上と同一**。違うのは名前と記録だけ） |

---

## 3. 監査者が作った参照されないコミット

監査側の申告どおり、次のコミットオブジェクトがリポジトリに実在します。

```
26d4b9e86f65c65ab008a53706d4810e8e87b4c3
  message : noop
  parent  : 0be7aaf…（現在の main）
  tree    : 04d1fb0…（現在の main と同じ）
  author  : Driedsandwich  2026-08-06T04:36:53Z
```

- **どの ref からも参照されていません**（branch / tag / PR の全 ref を API で列挙して確認）
- `main` と `v1.1.4` はどちらも `0be7aaf…` のままで、動いていません
- ツリーが現在の main と同一なので、内容が変わったところはありません

指示書のとおり、ref を作る・取り込む・cherry-pick する・掃除のために外部操作する、は
**どれもしていません**。参照されないオブジェクトなので、GitHub 側でいずれ回収されます。

---

## 4. やっていないこと

- commit / push / PR / タグ / GitHub Release
- ストアへのアップロード・提出・掲載文の変更・データ申告の入力
- Xへの実投稿
- 履歴の書き換え・秘密情報の失効処理（そもそも検出されていない）
- 実際のGitHub・実際のXの投稿画面での目視確認（アカウントが要り、誤投稿の危険がある）
- 監査側が独立に再実行していない項目（こちらの実測は macOS / Chrome 151 のみ）
